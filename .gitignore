# JS / Expo
node_modules/
.expo/
dist/
build/
web-build/
*.orig.*

# Environment — never commit real secrets
.env
.env.local
.env.production

# Certificates / signing
*.jks
*.p8
*.p12
*.key
*.mobileprovision
google-play-service-account.json

# iOS — track native files, but not CocoaPods build artifacts
# Run `cd ios && pod install` after cloning (setup.sh does this automatically)
ios/Pods/
ios/build/
ios/DerivedData/
*.xcuserstate
*.xcscheme

# Android — native dir is regenerated by EAS (continuous native generation)
android/
